server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/your-username/your-config-repo
          clone-on-start: true
  security:
    user:
      name: user
      password: password